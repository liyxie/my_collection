## 简单使用

```python
# 自带日志库
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    filename="app.log",
    datefmt="%Y-%m-%d %H:%M:%S",
    encoding="utf-8"
)

logging.info(f"数据：{data}")
```

