## vue项目

当你通过 FastAPI 的 StaticFiles 提供前端服务时，前端请求 /dc/auth/login 会被 FastAPI 处理，而不是被代理到 127.0.0.1:8800。

vue.config.js 里的 proxy 配置只在 npm run serve 开发模式下生效，打包成 dist 后就失效了。

FastAPI 应用只挂载了静态文件服务，没有配置 /dc 路径的代理转发。

